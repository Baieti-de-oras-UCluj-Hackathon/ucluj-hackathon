# Copy to .env — never commit real secrets
UMBRARO_ENV=development
JWT_SECRET=4XGJtAxnWzUBYRY4LKlYC/PVGyVOwaBFEvdqEvVyUPhq5qyxyzSdSZYDbGXcjTo2
DATABASE_URL=sqlite+aiosqlite:///./umbraro.db
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:8080,http://localhost:5555

# Firebase (optional — required only for /auth/firebase and /auth/register_with_firebase)
# Use the same project as the Flutter app. Point to a service account JSON file or leave
# empty to use Google Application Default Credentials in cloud environments.
FIREBASE_PROJECT_ID=uhack26-8050e
FIREBASE_CREDENTIALS_PATH=backend/secrets/service-account.json
